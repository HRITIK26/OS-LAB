#!/bin/sh
#is use for the comments $ it uses to denote variable and 1 and 2 is used for integer
#calculate the sum via command line argument
#$1 and $2 refers to the first and second arguments passed as command line
#calculate sum
sum=$(($1 + $2))
echo "sum is $sum"

